<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetech.raisetimeline.mapper.PostMapper">

    <insert id="insert" parameterType="com.raisetech.raisetimeline.domain.Post"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (user_id, content, image_path)
        VALUES (#{userId}, #{content}, #{imagePath})
    </insert>

    <select id="findById" resultType="com.raisetech.raisetimeline.domain.Post">
        SELECT * FROM posts WHERE id = #{id}
    </select>

    <update id="updateContent">
        UPDATE posts
        SET content = #{content},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM posts WHERE id = #{id}
    </delete>

    <select id="countNewerThan" resultType="long">
        SELECT COUNT(*) FROM posts WHERE id > #{afterId}
    </select>

    <select id="selectTimeline" resultType="com.raisetech.raisetimeline.domain.PostDetail">
        SELECT
            p.id AS id,
            p.content AS content,
            p.image_path AS image_path,
            p.created_at AS created_at,
            p.updated_at AS updated_at,
            u.id AS author_id,
            u.username AS author_username,
            u.display_name AS author_display_name,
            (SELECT COUNT(*) FROM comments c WHERE c.post_id = p.id) AS comment_count,
            (SELECT COUNT(*) FROM likes l WHERE l.post_id = p.id) AS like_count,
            EXISTS(SELECT 1 FROM likes l2 WHERE l2.post_id = p.id AND l2.user_id = #{currentUserId}) AS liked_by_me
        FROM posts p
        JOIN users u ON p.user_id = u.id
        ORDER BY p.created_at DESC, p.id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectDetailById" resultType="com.raisetech.raisetimeline.domain.PostDetail">
        SELECT
            p.id AS id,
            p.content AS content,
            p.image_path AS image_path,
            p.created_at AS created_at,
            p.updated_at AS updated_at,
            u.id AS author_id,
            u.username AS author_username,
            u.display_name AS author_display_name,
            (SELECT COUNT(*) FROM comments c WHERE c.post_id = p.id) AS comment_count,
            (SELECT COUNT(*) FROM likes l WHERE l.post_id = p.id) AS like_count,
            EXISTS(SELECT 1 FROM likes l2 WHERE l2.post_id = p.id AND l2.user_id = #{currentUserId}) AS liked_by_me
        FROM posts p
        JOIN users u ON p.user_id = u.id
        WHERE p.id = #{id}
    </select>

    <select id="selectNewerThan" resultType="com.raisetech.raisetimeline.domain.PostDetail">
        SELECT
            p.id AS id,
            p.content AS content,
            p.image_path AS image_path,
            p.created_at AS created_at,
            p.updated_at AS updated_at,
            u.id AS author_id,
            u.username AS author_username,
            u.display_name AS author_display_name,
            (SELECT COUNT(*) FROM comments c WHERE c.post_id = p.id) AS comment_count,
            (SELECT COUNT(*) FROM likes l WHERE l.post_id = p.id) AS like_count,
            EXISTS(SELECT 1 FROM likes l2 WHERE l2.post_id = p.id AND l2.user_id = #{currentUserId}) AS liked_by_me
        FROM posts p
        JOIN users u ON p.user_id = u.id
        WHERE p.id > #{afterId}
        ORDER BY p.id DESC
        LIMIT #{limit}
    </select>

</mapper>
