<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.raisetech.raisetimeline.mapper.CommentMapper">

    <insert id="insert" parameterType="com.raisetech.raisetimeline.domain.Comment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (post_id, user_id, content)
        VALUES (#{postId}, #{userId}, #{content})
    </insert>

    <select id="findById" resultType="com.raisetech.raisetimeline.domain.Comment">
        SELECT * FROM comments WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM comments WHERE id = #{id}
    </delete>

    <select id="selectByPostId" resultType="com.raisetech.raisetimeline.domain.CommentDetail">
        SELECT
            c.id AS id,
            c.post_id AS post_id,
            c.content AS content,
            c.created_at AS created_at,
            u.id AS author_id,
            u.username AS author_username,
            u.display_name AS author_display_name
        FROM comments c
        JOIN users u ON c.user_id = u.id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC, c.id ASC
    </select>

    <select id="selectDetailById" resultType="com.raisetech.raisetimeline.domain.CommentDetail">
        SELECT
            c.id AS id,
            c.post_id AS post_id,
            c.content AS content,
            c.created_at AS created_at,
            u.id AS author_id,
            u.username AS author_username,
            u.display_name AS author_display_name
        FROM comments c
        JOIN users u ON c.user_id = u.id
        WHERE c.id = #{id}
    </select>

</mapper>
